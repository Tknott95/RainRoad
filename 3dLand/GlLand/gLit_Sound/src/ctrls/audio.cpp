#include "../headers/audio.h"

void Audio::loadWavFile(ifstream& _file, Wav _wav, ALsizei& _size) {

};

Audio::Audio() {
  printf(" Audio Initialized...");
};

Audio::~Audio() {
  /* Delete all audio buffers here */
};

void Audio::Update() {
  /* FixedUpdate loop also? Better cals running both */
};